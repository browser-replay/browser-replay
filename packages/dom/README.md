# @browser-replay/dom

`@browser-replay/dom` is a virtual DOM library used by browser-replay to replay DOM mutations. It is a standalone library that can be used to create a virtual DOM tree and apply patches to the real DOM. It's used by browser-replay to optimize replay performance, especially when seeking.

See the [guide](../../docs/guide.md) for more info on browser-replay.
